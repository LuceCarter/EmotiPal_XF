<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:viewmodels="clr-namespace:EmotiPal.ViewModels" x:DataType="viewmodels:AnalyseImageResultPageViewModel"
             x:Class="EmotiPal.Views.AnalyseImageResultPage">
    
        
    <StackLayout>
        <StackLayout>             
           <Image Source="{Binding AnalysedPhoto}" />
             <Label Text="{Binding Description}"
            HorizontalOptions="CenterAndExpand"
            FontSize="24"
            VerticalOptions="Center"
			TextColor="{StaticResource PrimaryColorOrange}"/>         
        </StackLayout>
       
    </StackLayout>
        
    
   
</ContentPage>